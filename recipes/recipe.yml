---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: ublue-home
# description will be included in the image's metadata
description: This is my personal OS image.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/aurora
image-version: latest

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: rpm-ostree
    install:
      - corectrl
      - liquidctl
      - printer-driver-brlaser
    replace:
      - from-repo: https://copr.fedorainfracloud.org/coprs/danayer/linux-firmware-git/repo/fedora-41/danayer-linux-firmware-git-fedora-41.repo
        packages:
          - linux-firmware
      - from-repo: https://copr.fedorainfracloud.org/coprs/g/kernel-vanilla/next/repo/fedora-41/group_kernel-vanilla-next-fedora-41.repo
        packages:
          - kernel
      - from-repo: https://copr.fedorainfracloud.org/coprs/xxmitsu/mesa-git/repo/fedora-41/xxmitsu-mesa-git-fedora-41.repo
        packages:
          - mesa
      
  - from-file: flatpaks.yml
  - type: signing
